version: '3.8'

services:
  y2h-ppi-platform:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
      - ./logs:/app/logs
